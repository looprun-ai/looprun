# minimal-core — the run ledger

Run: the minimal-core program, branch `minimal-core` off main @ 7079285, unattended.
Dispatch: `docs/superpowers/plans/2026-08-30-minimal-core-execution-goal.md`
Spec of record: `docs/superpowers/specs/2026-08-30-minimal-core-free-author-proposal.md`
Rollback: tag `pre-minimal-core-2026-08-30` on looprun · agentspec · agentspec-bench.
Parked questions live in `minimal-core-DECISIONS.md`.

## Events

- 2026-08-30 23:45 — run started; branch `minimal-core` created at 7079285; controller online, opus subagents per step.
- 2026-08-31 00:05 — step 1 implementer landed a507c56: NEGATORS + helpers deleted, choiceFromUser ask-then-echo, 654 tests green, 14/14 ported; plain-names gate red confirmed PRE-EXISTING (main has the same 5-line set; branch carries a byte-identical subset of 4).
- 2026-08-31 00:15 — step 1 review: spec FAIL — Critical (latest-answer read inverted vs the engine's newest-first userTexts), 2 Important (question never closes; code minted from the contract instead of the question). Fix round 1 dispatched to the original implementer; the measured design (per-question mint + open/close lifecycle) ruled as the spec.
- 2026-08-31 00:15 — CONTROLLER RULING (for owner review): Portuguese/Japanese operator strings inside `choice-ask-then-echo.test.ts` are lawful — they are the measured object (operator input under test), not authored prose; the mechanism cannot be tested without a non-English operator. First instance in `packages/**`; flagged here rather than parked.
- 2026-08-31 00:30 — step 1 fix round 1: the guard now reads the standing question on a per-session ChoiceDesk (new `run/choice-desk.ts`; `choose` verdict kind; CallCtx.choices) — no userTexts read at all; per-question six-digit mint; consume-on-run. Commit recreated as ONE commit 5faab5d (amend hook-blocked; reset --soft + commit; a507c56 in reflog only).
- 2026-08-31 00:40 — step 1 re-review: spec PASS; one NEW Important — an `unknown` act left the answered question live (next record licensed with no ask).
- 2026-08-31 00:50 — step 1 fix round 2 + ACCEPTED at ade2892: consume fires on done || unknown (turn.ts:214 convention), red-first e2e test; 662 passed / 2 skipped, typecheck clean, gates clean except pre-existing plain-names (4 hits, subset of main's 5). Deferred minors on record: two choice guards on the same act+argument collapse onto one question (pathological authoring edge, for the final whole-branch review); the desk's ask-language behavior is the accepted microtest-4 limit, unmeasured on this engine.
- 2026-08-31 01:00 — step 2 implementer landed 3e58ff1: D1 in tryFinish (violation + same-prefix redrive), numbered owed facts + enumerated report, delivered-text figure walk both paths; deterministic layer red-first 10/13→13/13; r12 slice r1 5/5. Review: spec PASS + 5 Importants → fix round.
- 2026-08-31 01:20 — step 2 fix round + ACCEPTED at 22a4f19 (one commit, 43 files): claimedFactIsOwed (bogus id = violation) + EVERY_FACT fixture vacuity purged across 5 packages; identifier rule case-insensitive (A-05/BK-4402 never read as amounts; live proof "figure 5"→"id A-05"); harborpoint backup completed to all 5 originals (byte-identity validated); FLOOR_NAMES comment true. Slice r2 5/5, agreeing with r1 case for case. Redrive starvation NOT observed (D1 added violations, zero extra rounds; retries budget untouched).
- 2026-08-31 01:30 — step 2 residuals ON RECORD: (a) HONESTY SEAM, no owner — a finish claiming exactly the owed ids while stating none of their content passes deterministically; the measured answer is microtest-3's judged per-fact YES/NO auditor (17/20, 0 false accepts) and no step owns it — OWNER DECISION after the run; (b) identifier widening is INVENTED law (microtest ruler had lowercase+underscore only) — records-aware strip is the ruled fix, carried to step 3 with the currency shape (USD-500) and A-364 named; (c) minors deferred to final review: catalog.ts carries two disagreeing id-shape definitions; "A-04-A-05" re-exposes digits; small integers ground on date fragments; payingDesk fixture duplicated core/mastra; STATE_TAG newly in the desk prompt tail (step 3's ban covers it).
- 2026-08-31 02:00 — step 3 implementer landed 7c87a42: composer DELETED everywhere (reply-composer.ts gone, no second model prefix, tutorial/README/headers rewritten); engineClose = desk close-step, same conversation, warm prefix; bracketed-code ban in the close instruction; tool cards kept. Slice r1 5/5, STATE_TAG byte-leak gone. Review: spec PASS + 2 Important.
- 2026-08-31 02:30 — step 3 fix rounds: (1) id-strip BEFORE amount harvest — caught a real shipped invention live ("7 days" grounded only by berth code C-07; r2 refused and the desk rewrote); (2) CONTROLLER RULING: close charges message-facing rulers only (the three report-only rulers read a report engineClose discards — charging them floored a natural paragraph; seal-the-report alternative parked as D-1); (3) wallEcho open-question exemption narrowed to the owed ask sentence; (4) any [F…] tag unspeakable; (5) BACKLOG row 51 rewritten; (6) CLOSE_REDRIVES restored to the MEASURED 2; (7) refused close attempts now dump their text (closeRefused rows).
- 2026-08-31 02:50 — step 3 ACCEPTED at 1f9ef80: 692 passed / 2 skipped; spec acceptance GREEN (consent turns deliver desk prose, floorDeliveries 0 = flat, no composer in delivery.by, byte guarantee holds in every run). Slice r3 4/5: end-a-season t2 delivers fluent prose asserting the WRONG branch (r016's letter) — a close-path truthfulness residual NO mechanism catches (the judged pass does not run on close; microtest-6 priced the desk at 11/12 truthful). PARKED as D-3 (a: judged close pass / b: seal+extend report rulers / c: accept priced residual — shipped c). On earlier runs the same turn FLOORED and the letter held by accident; step 7 measures letters AND floors together. STATE_TAG paraphrase class parked as D-2 (stable, desk-authored, byte guarantee intact).
- 2026-08-31 03:20 — step 4 BLOCKED correctly (the briefed file has no client; nothing POSTs /completion) → CONTROLLER RULING: the tier DECLARES `cache_prompt` in packages/models, the single funnel (mastra-model-port) APPLIES it, local llama.cpp targets only (gemini body byte-identical, pinned). `slots 2→1` → `--parallel 1`.
- 2026-08-31 03:50 — step 4 ACCEPTED at 9fc43d7: 695 passed / 2 skipped, red-first proven by revert on both fix tests; providerOptions threaded through the three doors (exam-runner, routed-agent, RoutedSubjectCfg) so a local seat can carry the flag; per-namespace merge (a target's safetySettings survive the preset); unverified links stated AS-IS in comments (provider id must begin with `llamacpp`; cache_prompt is a /completion field vs the /v1/chat/completions client) — BOTH are BACKLOG row 8's live measurement. Layout untouched (verified: no prompt/turn/delivery file in the diff).
- 2026-08-31 04:00 — step 4b ACCEPTED at f5020d5 (+ agentspec-bench dec5b48, tools-only, UNPUSHED): tape law 1 landed (window-2 rewrite deleted; append-only pinned by byte-freeze tests, red-first); byte cuts honest — every ruler-visible family is protected by a standing law (the _dup2 8,238 B are 79% guard-rules-on-their-card + 21% declared prose; _dup is 100% paraphrase of declared content; _schema already slim), so the levers landed were engine-created duplication only: one numbered fact list per close request (−642 B×3/turn; also a live correctness fix — F-ids no longer mean two things in one request) + the duplicate finish order (−107 B). THE LAW FIRED: a 59 B clause cut cost letter r1 (the desk stopped naming Rui Baptista) and was REVERTED with byte-level attribution — a cut that costs a point is not a cut. Live defect found AND closed: a bare-code turn skipped the language check (the desk answered an English conversation in SPANISH, counter 0) — fixed to read the last operator message clearing the letter floor; proven live (mismatch caught, redriven, delivered in ENGLISH, languageMismatches 1). Slice final 4/5 = baseline, new losses 0; end-a-season flaps (D-3 parked, variance). Tape laws 2-3 (budget compaction + summary) NOT built — engine has no compaction; measured net-negative at 24 turns; design recorded on BACKLOG row 1. The −50% target: distance recorded on row 1 with four tail duplications named (need a tail ruler first). Bench rulers repaired (_finish binds the close-step seam; English output).
- 2026-08-31 05:00 — step 5 ACCEPTED at looprun b842017 + agentspec fda5b5a (main, ahead 1, UNPUSHED): judge rows carry guardName · detail · finish.message · delivery.by (live-verified on the slice: r005's floored close read stage-by-stage from the row alone — French reply refused by the reader, English rewrite refused 3× on fact ids, floor delivered); judged pass opt-in per desk (judgePass), CardCheck refuses a judged guard on a desk that buys no pass; the judge keeps its OWN system prefix and is never the voice that wrote the reply it reads (the desk transcript rides as evidence — the seams page states it precisely). references/engine-seams.md ships in the skill; the skill's own gate flipped red→green with the choiceFromUser terms→options teaching repair (step-1 debt paid). Both subject trees git-init'd + seal()'d, zero key material in any blob (verified). Slice 4/5 = baseline, no new loss. Deferred: hand-written-card judgePass lint is CardCheck-owned now; F7-F12 advisories recorded for the final review.
- 2026-08-31 06:30 — step 6 ACCEPTED at looprun 2b42c4f + agentspec d040faf (main, UNPUSHED) + harborpoint 4bff417/528ebbb: the emitter SHRINKS — sentence-shape policing (skeletons, selector, six-voice MANDATE) leaves, prose goes free in the declaration; pairing/slots/checkAgainstSurface/rule+deny stay; cards.ts stays generated (re-emit byte-identical, verified independently). FOUR new factories (argCondition · valueFromUserOrRecord · argMatchesRecord · onlyAfterWhen), each named back against the surface on every mis-pointing (the review caught a DEAD LAW first build: `in:` compared the list to itself and licensed every value — fixed, refusal pinned; and the target-without-entity gap closed across three factories at once). Word-list + world-id lints red on fixtures and wired into runGate; strict subject tsconfig WIRED INTO the subject gate (proven red on a plant); certify() refuses held-out-touched repairs and test.md now instructs the repairs.jsonl append (the writer exists); label() recursive over field names at every depth (microtest-10's exact plant marked), values byte-identical, no per-turn judge. author.md = the one authoring lesson (declare.md + ladder chapters merged and deleted, six voices advisory, T-loop pointed); skill gate 24/24. Residuals on record: word-list lint sees 2 of 8 shapes (the spec's own residual — review catches what lints cannot); label prefix inflates deep record lines (watch step-7 tokens); from:-typo warning reversed by measurement (false refusal on record families no act targets).
- 2026-08-31 06:50 — step 7 STARTED: three parallel opus regression agents dispatched — harborpoint (full 33, target ≥28 with the 5 paid), trialworks (29/29 holds; deliberate choiceFromUser migration recorded), atlas-c20 (routed ladder 12→40→100, target ≥92; deliberate migration recorded; the 3×-both-columns re-certification of 92-vs-78 is NOT this run's gate and stays OPEN unless time clearly allows). Step-8 blind dispatch prepared (fresh clean-context agent; sees only the shipped skill + subjects/atlas/ask/inputs; auto-runs when step 7 closes).
- 2026-08-31 07:30 — step 7 harborpoint: 26/33 vs r12's 28/33 (28/33 on the mechanism-comparable set). PAID: r005 (the 364 stays dead — "the total is settled on the invoice") and r010 (berth code B-01 named). NOT paid: r001/r003 (same shape as r12 — rotating, unpaid there too), r016 (wrong-branch letter, parked D-3, now also leaking onto r032). Root-caused regressions: 2 rows = D-4 case-script debt (fuel cases lack the echo turn the ruled choice law now requires; 1 invariant failure = unrunnable one-turn script); 2 rows = readIsSpoken redriving question-only turns into floor dumps (engine defect, fix round pending); languageMismatches fired 3× ALL FALSE (English↔English) while a DANISH delivery on a consent turn moved no counter (desk path unchecked). Tokens +4.7% input (label() did NOT inflate); one 65k-token runaway generation (no output cap). Evidence committed fb76984.
- 2026-08-31 07:30 — step 7 trialworks: 26/29 vs 29/29 baseline (27/29 comparable; 1 case = D-4 script debt). The two real drops are desk-finish quality classes (asked for the wrong field; stated the passing field and omitted the WITHDRAWN the case turns on — readIsSpoken satisfied by the id alone). Migration proven NOT the cause (rendered prompts byte-identical pre/post). THE COST WIN IS REAL: input −18.0%, output −31.6%, model calls 133→93. Migration 0ca6afc; evidence f056ac2.
- 2026-08-31 09:55 — step 7 CLOSED. Two tier-driven fix rounds landed as ONE looprun commit fe233d3 (+ agentspec 78f6024): (1) the {answer} dynamic case turn (a script answers a choice the way it approves a consent) + static validation + the skill teaching it; (2) proseDropsReads keeps read-id semantics but NEVER floors (redrive-then-deliver-marked — the ask survives, the guard keeps teeth); (3) engine [F…] labels stripped before the figure walk (the F1/F2-read-as-figures floor is gone, spot-proven live); (4) THE LANGUAGE RULING: the trigram cut is DELETED — measured classes INTERLEAVE (English 0.1687 refused-would-be vs Portuguese 0.1718 delivered; no cut separates same-script short texts) and the whole-conversation reference decays (Danish passes one message later; 22/186 conversations) — the deterministic check now holds only SCRIPT classes (CJK/Cyrillic/etc. vs latin refuses; latin↔latin ABSTAINS, the accepted limit, lieCheck's half), verified over 863 delivered rows: 0 false refusals. The OLD check was itself the cause of two sealed-record anomalies (c20 case 57's French reply; case 86's self-contradiction) — both gone.
- 2026-08-31 09:55 — step 7 SCORES OF RECORD (r2 runs): harborpoint 28/33 = r12 baseline (28/31 comparable; r005+r010+r016 paid; r001/r003 rotate unpaid as in r12; evidence 302ae6f). trialworks 25/29 (25/28 comparable; the one move is a receipt+refusal D-3-family self-contradiction on a repeated acting call, judged strictly and flagged overrulable; the fix paid 2 floors away; evidence c70b86b). atlas-c20 routed ladder 11/12 · 35/40 (beats sealed 32/35 on comparable) · 85/100 raw, 79/88 comparable = 89.8% vs ≥92 (sealed same-set 84/88); language clean over the hundred; the gap's biggest driver is the choice mechanism reaching past the D-4 twelve — the desk VOLUNTARILY enters gated acts (61/77/82) and delivers a code prompt instead of the answer (D-4's ruling must cover this); evidence c3c45a6. The 3×-both-columns re-certification of 92-vs-78 stays OPEN (not this run's gate). Cross-cutting residuals on record: the engine's ask-instruction text reaches the operator verbatim on some paths; the desk once denied a tool the house has (atlas 71); a cap applied without naming the refused figure (atlas 75); "4,8000" read as 48000; a 65k-token runaway generation reproduced 2× (no output cap); label() inflates per-call input on deep-nested records (+17-19% c20) and saves on flat ones (−18% trialworks).
- 2026-08-31 00:15 — NOTE: the agentspec skill still teaches the old `terms` surface; the split lands at steps 5/6 per the spec (PREPARED → COMPLETE). Step 8 already depends on step 6. Step 7's pin updates must migrate each subject's generated cards.ts to the new choiceFromUser surface — recorded as part of the deliberate pin update.

## FINAL-REPORT

The unattended run executed steps 1-8 of the minimal-core program on branch
`minimal-core`. Every engine step landed, was adversarially reviewed, and carries its
fix rounds folded into ONE commit per step. Nothing was pushed except this branch.

### Steps landed

| step | commit | one line |
|---|---|---|
| 1 | ade2892 | choiceFromUser is ask-then-echo on a per-session ChoiceDesk; NEGATORS and its helpers deleted; 14/14 ported |
| 2 | 22a4f19 | D1 — owed facts (numbered) gate the desk's own message, same-prefix redrive; figure walk both paths; r12 slice 5/5 twice |
| 3 | 1f9ef80 | D2′ — composer DELETED everywhere; desk close-step, message-facing funnel, 2 measured redrives; byte guarantee on every run |
| 4 | 9fc43d7 | cache_prompt declared by the local tier, applied at the single funnel; --parallel 1; layout untouched |
| 4b | f5020d5 | tape append-only (window-2 rewrite dead); one fact list per close; the 59 B cut that cost a letter REVERTED by the law; bare-code turns keep their language |
| 5 | b842017 | judge rows name their stage (guardName·detail·finish.message·delivery.by); judgePass opt-in; engine-seams.md in the skill; both subject trees git-init'd + sealed |
| 6 | 2b42c4f | emitter SHRUNK (prose free, checks factory-only, 4 new rungs each named back against the surface); word-list+world-id lints; strict subject tsconfig wired into the gate; certify() held-out refusal; recursive label(); author.md = the one lesson |
| 7 | fe233d3 | the tier's fixes: {answer} case turn; unspoken-read never floors an ask; [F…] labels are not figures; the language check holds SCRIPT classes only (the trigram cut measured unseparable and deleted) |

agentspec (main, UNPUSHED): d040faf → 78f6024. agentspec-bench: migration + run evidence commits, UNPUSHED. harborpoint/trialworks: git-init'd this run, sealed, evidence committed, no remotes.

### The step-7 regression table (scores of record, r2 runs on the tier's fixed build)

| subject | score | comparable | baseline | verdict |
|---|---|---|---|---|
| harborpoint | 28/33 | 28/31 | r12 28/33 | HOLDS the baseline; r005/r010/r016 paid; r001/r003 rotate unpaid as in r12 |
| trialworks | 25/29 | 25/28 | 29/29 | 3 real drops: 1 strict-judged D-3-family self-contradiction (flagged overrulable) + 2 desk-finish quality classes; cost −18% input/−31.6% output |
| atlas-c20 routed | 85/100 | 79/88 = 89.8% | sealed 96/100 (same-set 84/88) | UNDER the ≥92 target by 2.2 comparable points; biggest driver = the choice mechanism reaching past D-4 (desk voluntarily enters gated acts); language clean over the hundred |

The 92-vs-78 governed-vs-traditional claim was NOT re-certified (needs 3× both columns; open).

### Parked for the owner (minimal-core-DECISIONS.md)

- D-1 seal-and-charge the close-step report, or keep it discarded?
- D-2 STATE_TAG paraphrase class: mechanical fold or judged-only?
- D-3 the close path can state a falsehood no mechanism catches (wrong-branch tense/actor class; measured; grew: receipt+refusal contradictions, rule text riding into operator sentences via the refusal-frame exemption) — a/b/c options recorded.
- D-4 sealed exam scripts predate ask-then-echo (3 cases across 2 subjects unrunnable/short; AND the ruling must cover the desk voluntarily entering gated acts — atlas 61/77/82). The {answer} turn + validation EXISTS now; the case-script edits await the ruling.
- c20 cannot be emitted by anyone (7 mutating acts carry no after:) — pre-existing on main, surfaced by the step-6 emitter checks.

### Step 8 — blind atlas-c21

IN FLIGHT at report time: a fresh clean-context opus agent authoring
`agentspec-bench/subjects/atlas-c21` from zero under the shipped skill (78f6024) on
the shipped engine (fe233d3), whole pipeline to T-loop close, then ungoverned + certification.
Its running log: the session scratchpad's `step8-blind-REPORT.md`. Its result is appended
below this section when it lands.

### To resume / review

```
cd ~/Dev/js/looprun/looprun && git checkout minimal-core   # everything is here
# rollback of everything: tag pre-minimal-core-2026-08-30 (all three repos)
# unpushed: agentspec main (78f6024), agentspec-bench main (evidence),
#           harborpoint/trialworks local git repos (no remotes)
```
