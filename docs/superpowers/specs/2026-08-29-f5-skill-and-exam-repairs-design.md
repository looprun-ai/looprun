# F5 — The Skill's Corrections and the Exam's Repairs

**Status:** DRAFT — pending owner review
**Program:** `2026-08-28-natural-voice-recovery-design.md` — inserted by the owner's
ruling between F3 and F2, so F2's single campaign measures a clean ruler on the final
build.
**Scope and authorization:** this phase IS the owner's express unlock of the skill,
scoped to backlog rows 2, 4, 5, 6 and 7 of `agentspec/BACKLOG.md` — nothing else on the
pages. Runs AFTER F3 (the loop needs engine and skill in agreement).

---

## 1 · The measurement — what each row costs

- Rows 5–7 (cases 76, 61/62/68, 48) cost F2's ruler up to five cases that fail for
  reasons already diagnosed and registered: an expectation with no written rule (76),
  and questions that demand what correct conduct does not deliver (61, 62, 68, 48).
  Evidence: identical failures across the pinned baseline, the unpinned baseline and
  the F1 certification (`…/2026-08-29-f1-cert100/T-REPORT.md`).
- Rows 2 and 4 cost every future blind round: nine known teachings a coding agent had
  to learn from refusals instead of pages.

## 2 · The exam repairs (rows 5–7) — each fix, each validation

**Case 76 — the missing booking-restraint rule.** Repaired THROUGH the loop, as the
owner confirmed is its job: the loop reads the failed evals and adds the desk law to
`atlas-c17`'s declaration.yaml on the booking counter —

> Open a booking only when the operator asked for one in this conversation; a booking
> you offered waits for their yes before it is opened.

Hand edits are hypothesis tests only; the shipped wording comes from running the
repair round. Validation: cases 76, 02, 44, 59 and 67 re-run (the restraint must not
refuse bookings that WERE asked — 02/67 book on request, 44/59 quote and ask).

**Cases 61, 62, 68, 48 — questions redesigned or retired, each with its reason
written.** The dispute of each, and the repair direction:

| case | the dispute | the repair |
|---|---|---|
| 61 | expects a booking made while the desk lawfully asks the delivery choice first | the rubric accepts the lawful ask as the first reply, or the turn supplies the choice |
| 62 | expects a customer read the desk answers without | the invariant demands the read the rubric already implies (`listCustomers`), and the rubric row states the read's answer must be quoted from it |
| 68 | expects the fleet desk to defer a claim the router lawfully hands to claims | the route expectation follows the amended manual (`CASE_ROUTE`), as 48's family already does |
| 48 | wants the missing dispatch permission named by a desk that cannot reach the act | the rubric row asks the sentence of the desk that receives the request, per the registered redesign note |

Validation: the four repaired cases plus their family neighbors (79, 78, 81) re-run
on the current engine; every letter of the new rubric read in session; a repaired
case's commit states the old demand, the new demand and why.

## 3 · The page corrections (rows 2 and 4)

**Row 2 — the five missing teachings, one sentence each, on the page where its moment
lives:**
1. `reads: record` works only on acts that carry a target — taught beside the factory,
   not learned from the emit's refusal;
2. which factories DECIDE an acting tool and which never do (`onlyAfter`,
   `checkResult`, `prose` do not) — stated in the catalog's ladder;
3. a `needs` alias must name a read inside the holding desk's own lane — stated at N5;
4. no channel speaks a boolean answer — the tenses ban booleans, and the page says
   what an author does when a case turns on one;
5. `later`'s scope on non-consent acts — defined where `later` is taught.

**Row 4 — the four findings of the c12 coding agent, verified one by one against the
current pages:** the currency-mark carve-out on `valueFromUser` (a "R$ 100" the tool
receives as `100` is the same figure); `precondition`'s target requirement taught at
the declaration page; read-law homing taught earlier in the reading path; result
shapes visible somewhere a page owns. Whatever a current page already answers closes
with a pointer; what none answers is fixed with one sentence, same as row 2.

Validation: every page edit is A/B-guarded by its OWN case — for each teaching, one
directed engine-level fixture (the shape the teaching prevents) added to the emit or
core test suite where the refusal lives, so the teaching's claim about the engine is
asserted by a test the same day the sentence lands. The behavioral gate of the pages
themselves — can a fresh coding agent apply them? — is the next blind round, which
these corrections unblock and which rides after F2 as its own program.

## 4 · The order inside the phase

1. Exam repairs first (rows 5–7), each with its directed re-run — the ruler is fixed
   before anything else is measured against it;
2. then the page corrections (rows 2 and 4), each with its fixture;
3. close each backlog row as it lands — a row leaves only done or retired with its
   reason.

## 5 · What F5 does not touch

The engine (F3 closed it), the skill's other pages, backlog rows 1, 3 and 8–14, and
any full-hundred measurement — that is F2's single campaign, which runs only after
every row here is closed.
