[
  {
    "name": "lookupMember",
    "description": "Find a member by name.",
    "parameters": { "type": "object", "properties": { "query": { "type": "string" } }, "required": ["query"] }
  },
  {
    "name": "listRooms",
    "description": "List reading rooms.",
    "parameters": { "type": "object", "properties": { "date": { "type": "string" } } }
  },
  {
    "name": "reserveRoom",
    "description": "Reserve a room for a member.",
    "parameters": {
      "type": "object",
      "properties": {
        "roomId": { "type": "string" },
        "memberId": { "type": "string" },
        "date": { "type": "string" },
        "startTime": { "type": "string" },
        "endTime": { "type": "string" }
      },
      "required": ["roomId", "memberId"]
    }
  },
  {
    "name": "registerVisitor",
    "description": "Register a walk-in visitor.",
    "parameters": { "type": "object", "properties": { "name": { "type": "string" } }, "required": ["name"] }
  },
  {
    "name": "getInvoice",
    "description": "Fetch the latest membership invoice.",
    "parameters": { "type": "object", "properties": { "memberId": { "type": "string" } } }
  }
]
