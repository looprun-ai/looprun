# deps / build
node_modules/
dist/
*.tgz

# env / local
.env
.env.local
.DS_Store

# eval scratch (committed artifacts are *.judged.json, cert.json, CERT.md only)
eval-results/**/*.dump.json
eval-results/**/*.tasks.jsonl
eval-results/**/*.autofail.json
eval-results/**/*.verdicts.jsonl

# governance proof scratch (raw vitest JSON + run summary)
governance/.artifacts/*
!governance/.artifacts/.gitkeep

# eval battery output (a baseline artefact the operator keeps where they want it)
packages/eval/.battery/

# logs
*.log

# mastra dev scratch
.mastra/
