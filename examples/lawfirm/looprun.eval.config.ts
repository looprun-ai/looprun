/**
 * The lawfirm eval contract — the generated bundle + the generated eval set + the world factory.
 * Generated by the agentspec skill (A→G→E→N); the measured loop (T/S) runs from here.
 */
import type { EvalConfig } from '@looprun-ai/eval';
import { SPECS, THEME } from './src/agents/lawfirm/index.js';
import { TOOL_DEFS } from './src/world/tools.js';
import { worldFactory } from './src/world/world.js';
import { CASES, CASE_MAP } from './evals/cases.js';

export default {
  domain: 'lawfirm',
  specs: SPECS,
  theme: THEME, // each spec also carries spec.theme (same object)
  worldFactory,
  toolDefs: TOOL_DEFS,
  cases: CASES,
  caseMap: CASE_MAP,
  judgePromptPath: 'evals/judge-prompt.md',
  bar: 0.9,
} satisfies EvalConfig;
