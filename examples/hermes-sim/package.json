{
  "name": "hermes-sim-example",
  "version": "0.0.2",
  "private": true,
  "type": "module",
  "description": "End-to-end integration sim: the REAL Hermes-Agent harness talks to looprun agents exposed as OpenAI-compatible models (@looprun-ai/server); every tool call lands in deterministic fake worlds and the end-state is asserted.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "sim": "tsx src/run-sim.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ai-sdk/google": "^3.0.0",
    "@ai-sdk/openai": "^3.0.0",
    "@looprun-ai/server": "workspace:*",
    "@mastra/core": "^1.42.0",
    "ai": "^6.0.184",
    "looprun": "workspace:*",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
