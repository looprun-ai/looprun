/**
 * looprun.eval.config.ts — the eval contract of this project (generated by the agentspec skill).
 * Wires the generated bundle: SPECS + THEME, the deterministic world, the tool surface, and the
 * debate-validated eval set with its caseMap (every case exactly once).
 */
import type { EvalConfig } from '@looprun-ai/eval';
import { SPECS, THEME } from './src/agents/atlas/index.js';
import { TOOL_DEFS } from './src/world/tools.js';
import { worldFactory } from './src/world/world.js';
import { CASES, CASE_MAP } from './evals/cases.js';

export default {
  domain: 'atlas',
  specs: SPECS,
  theme: THEME, // every spec also carries spec.theme — this is the host-level default
  worldFactory,
  toolDefs: TOOL_DEFS,
  cases: CASES,
  caseMap: CASE_MAP,
  judgePromptPath: 'evals/judge-prompt.md',
  bar: 0.9,
} satisfies EvalConfig;
